// variables.js

// Using var
var name = 'John';
console.log('Name (var):', name);

// Using let
let age = 30;
console.log('Age (let):', age);

// Using const
const pi = 3.14;
console.log('Value of pi (const):', pi);

// Uncommenting the next line will cause an error because `pi` cannot be reassigned
// pi = 3.14159;
